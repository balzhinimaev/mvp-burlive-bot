services:
  bot:
    image: ghcr.io/balzhinimaev/mvp-burlive-bot:latest
    container_name: buryat-bot
    restart: unless-stopped
    
    env_file:
      - .env
    
    environment:
      - NODE_ENV=production
    
    ports:
      - "8080:8080"
    
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"